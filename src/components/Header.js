import React from 'react';
import styled from 'styled-components';

const Header = styled.div`
  margin-left: auto;
  margin-right: auto;
  max-width: 500px;
`;

export default () => (
  <Header>
    Nag Me
  </Header>
);
